services:
  apartments-ranking:
    build: .
    container_name: apartments-ranking
    restart: unless-stopped
    ports:
      - "8080:3000"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
